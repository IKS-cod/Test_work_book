version: '3.8'

services:
  postgres:
    image: postgres:16
    container_name: test_work_books
    environment:
      - POSTGRES_DB=tw2
      - POSTGRES_USER=student
      - POSTGRES_PASSWORD=chocolatefrog
    ports:
      - "5433:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
